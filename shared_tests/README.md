# shared_tests

Shared datasets and manual regression checks used across both the old prototype
and the new annotator.

## Structure (to be populated as phases progress)
- `fixtures/`  — small reference images + label files for unit tests
- `regression/` — manual regression checklists per phase
- `datasets/`  — larger sample datasets (gitignored if large)
